name: Trigger auto deployment for npact-album-api-test

# When this action will be executed
on:
  # Automatically trigger it when detected changes in repo
  push:
    branches: 
      [ main ]
    paths:
    - 'src/**'
    - '.github/workflows/npact-album-api-test-AutoDeployTrigger-1cfba7fc-9738-4105-b072-f0c505064844.yml'

  # Allow manual trigger 
  workflow_dispatch:
      
jobs:
  build-and-deploy:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout to the branch
        uses: actions/checkout@v2

      - name: Azure Login
        uses: azure/login@v1
        with:
          creds: ${{ secrets.NPACTALBUMAPITEST_AZURE_CREDENTIALS }}

      - name: Build and push container image to registry
        uses: azure/container-apps-deploy-action@v1
        with:
          appSourcePath: ${{ github.workspace }}/src 
          registryUrl: cade76b17cc1acr.azurecr.io
          registryUsername: ${{ secrets.NPACTALBUMAPITEST_REGISTRY_USERNAME }}
          registryPassword: ${{ secrets.NPACTALBUMAPITEST_REGISTRY_PASSWORD }}
          containerAppName: npact-album-api-test
          resourceGroup: rg-dev-fims-central-001
          imageToBuild: cade76b17cc1acr.azurecr.io/npact-album-api-test:${{ github.sha }}
          dockerfilePath: Dockerfile

     

